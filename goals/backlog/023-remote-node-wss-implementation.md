# Backlog: Remote Node WSS Implementation

Implement the reverse WSS sidecar connection with scoped node auth, heartbeat, command envelope validation, approval boundaries and audit events. Keep destructive execution disabled until explicitly authorized.
