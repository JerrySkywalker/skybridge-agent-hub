# Backlog: PowerShell Shared Redaction Rules

Update Codex hook and runner PowerShell scripts to consume `packages/event-schema/src/redaction-rules.json` directly, keeping TypeScript and PowerShell redaction behavior aligned without duplicating pattern lists.
