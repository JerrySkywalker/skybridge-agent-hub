{
  "name": "@skybridge-agent-hub/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@skybridge-agent-hub/event-schema": "workspace:*",
    "@skybridge-agent-hub/notification-ntfy": "workspace:*",
    "fastify": "^5.3.3",
    "nanoid": "^5.1.5",
    "zod": "^3.25.0"
  }
}
