# Local Doctor Action Guide

The doctor action guide explains issues with safe next actions:

```powershell
pwsh -ExecutionPolicy Bypass -File .\scripts\powershell\skybridge-local-doctor.ps1 -Command action-guide
```

Every action includes severity, explanation, safe next action, `destructive_action_required=false`, `host_mutation_required=false`, a suggested command preview and a docs link. token_printed=false
