# Manual Install Preview

`scripts/powershell/skybridge-manual-install-preview.ps1` is a preview-only planner.

It can describe copy, shortcut, Start Menu and PATH plans, but it does not copy files outside `.agent/tmp`, create shortcuts, write Start Menu entries, mutate PATH, write registry keys, create scheduled tasks, create services, modify power settings or use the network.
