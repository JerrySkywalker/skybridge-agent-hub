# Trusted Docs Auto-merge Disabled By Default

Trusted-docs auto-merge is not enabled.

- No default automatic task PR merge exists.
- No UI exposes an enabled auto-merge button.
- No command in the trusted-docs preview gate applies a merge.
- Human review remains required.
- Branch protection and GitHub repository settings remain operator-owned.
- Generic bounded queue apply, remote execution, and arbitrary command dispatch remain disabled.
- `token_printed=false`.
