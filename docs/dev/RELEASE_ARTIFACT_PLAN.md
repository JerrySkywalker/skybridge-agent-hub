# Release Artifact Plan

The local release artifact plan is a preview contract for future packaging. It does not publish or install anything.

Planned metadata:

- product: SkyBridge Agent Hub
- package scope: `@skybridge-agent-hub/*`
- Desktop build output: `apps/desktop/dist`
- Tauri metadata: `apps/desktop/src-tauri`
- channel: `local-preview`

Deferred actions:

- GitHub release creation
- artifact upload
- installer signing
- host installation
- autostart registration

token_printed=false
