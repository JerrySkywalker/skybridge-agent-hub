# Local Launcher RC Release Notes

Added:

- repo-root launcher entrypoints;
- local launcher command router;
- session supervisor report and next-action guide;
- safe operator walkthrough;
- demo fixtures and docs;
- Web/Desktop launcher and walkthrough panels;
- validation smoke pack.

Still disabled: Codex worker, workunit apply, task creation, task claim, task PR creation, generic queue apply, remote execution and arbitrary command dispatch. token_printed=false
