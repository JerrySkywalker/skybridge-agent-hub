# Portable Config Profile

`fixtures/productization/portable-config.example.json` defines `skybridge.portable_config.v1`.

The profile is preview-only and contains no secrets, tokens, raw pairing codes or host absolute secret paths. Local auth and pairing are represented as disabled preview metadata only.
