# Manual Session RC Release Notes

Added:

- `scripts/powershell/skybridge-local-session.ps1`;
- `scripts/powershell/skybridge-local-doctor.ps1`;
- fixture-only bounded start/stop smokes;
- local session Web and Desktop panels;
- safe operator demo mode;
- runbooks for preview, bounded apply, doctor and troubleshooting.

Still disabled: Codex worker, workunit apply, task creation, task claim, task PR creation, generic queue apply, remote execution and arbitrary command dispatch. token_printed=false
