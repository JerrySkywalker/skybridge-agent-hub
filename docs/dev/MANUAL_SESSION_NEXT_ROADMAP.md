# Manual Session Next Roadmap

Recommended follow-up goals:

- authenticated local pairing for manual session controls;
- operator-approved service-install preview with explicit human gate;
- richer port selection without default port collisions;
- browser visual QA for the local-session Web route;
- Desktop bridge read model for `.agent/tmp/local-session` reports;
- signed installer planning without auto-install or network update.

Do not enable worker execution or queue apply as part of these follow-ups without a separate explicit safety goal. token_printed=false
