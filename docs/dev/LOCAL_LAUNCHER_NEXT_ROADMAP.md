# Local Launcher Next Roadmap

Recommended follow-ups:

- Browser visual QA for the launcher route;
- local read model for launcher reports in Desktop;
- configurable preview port selection;
- authenticated local pairing for future approved controls;
- signed installer planning in a separate explicit safety goal.

Do not add worker execution or queue apply to the repo-local launcher without a separate explicit authorization goal. token_printed=false
