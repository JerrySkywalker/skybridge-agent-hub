# Campaign Compiler Pilot 002

This deterministic docs-only pilot file is reserved for Mega Goal 322 live validation.

Allowed task:

- `campaign-policy-compiler-pilot-docs-002`

Safety boundary:

- docs-only update;
- no deployment;
- no credentials or private configuration;
- no server-root or external infrastructure changes;
- no repository settings changes;
- selected worker `jerry-win-local-01`;
- `token_printed=false`.

Operator note:

- Use this pilot only to validate that Mega Goal 322 campaign compilation can select a single allowed docs task for `jerry-win-local-01`.
- Treat the compiled campaign output as review evidence, not as deployment approval.
- Stop after the allowed task is recorded; do not chain into code, script, credential, deployment, repository-setting, or infrastructure changes.
- A successful run should leave this file as the only intended repository change for pilot 002.
