# Run-Until-Hold Pilot 003

This deterministic docs-only pilot file is reserved for Mega Goal 321 fixture-only max-limit and hold validation.

Allowed task:

- `run-until-hold-pilot-docs-003`

Safety boundary:

- docs-only update;
- no deployment;
- no secrets;
- no server-root or external infrastructure changes;
- no GitHub settings changes;
- selected worker `jerry-win-local-01`;
- `token_printed=false`.
