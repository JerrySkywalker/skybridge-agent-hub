# Copy this file to $HOME\.skybridge\hermes.env.ps1 and replace placeholders locally.
# Do not commit real Hermes API values.

$env:HERMES_API_BASE = "http://127.0.0.1:18080"
$env:HERMES_API_KEY = "replace-with-local-hermes-api-key"
$env:HERMES_MODEL = "replace-with-safe-default-model-if-needed"
